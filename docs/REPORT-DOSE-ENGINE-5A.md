# REPORT — 正向剂量计算引擎选型报告与原型验证（P3-M5a）

日期：2026-09-09 · 状态：M5a 已交付（选型结论 + 可运行原型）
关联：PLAN-PHASE3.md P3-M5 · 验证基准 = 研究 1（TEST849，NPC 9 野 IMRT，
73.92 Gy / 33 fx，外部 Eclipse 剂量网格 94×182×87 @ 2.5 mm，max 8084.8 cGy）

---

## 1. 术语与定位

正向剂量计算 = **给定束流（几何/能量/通量）与患者 CT，计算三维剂量网格**。
AAA、Acuros XB、蒙特卡洛都属于这一层。逆向优化（M5b）是另一层：以 DVH 目标
为输入、以正向引擎为内核迭代求解通量/叶位。**没有 5a 就没有 5b**。

## 2. 候选算法对比

| 候选 | 原理 | 相对精度 | 计算量 | 自研工程量 | 依赖 | 结论 |
|---|---|---|---|---|---|---|
| Pencil Beam（解析水等效）| 射线数学深度 × 离轴剖面 × 平方反比 | 低（均匀介质 ±3-5%；不均匀介质差 5-10%+）| 极低（本原型 9 野全网格 7.3 s）| **数天（已交付）** | 无 | ✅ v1 原型 |
| Collapsed Cone 卷积叠加 | 能量沉积核叠加 + 射线扇形密度缩放 | 中（均匀 ±2%；不均匀 ±3-4%）| 中（需核库 + 逐体素叠加，Node 下分钟级或需优化）| 数周-数月 | 核数据（可从公开文献/代码取得）| ⏭ v2 推荐 |
| Monte Carlo | 逐粒子随机输运 | 金标准（±1-2%）| 极高 | 数月-年 + 物理审校 | 完整截面库 | ❌ 自研不现实；仅评估集成 |
| LBTE（Acuros XB 类）| 确定性求解玻尔兹曼方程 | 接近 MC | 高 | 数月-年 | 学术实现稀少 | ❌ 同上 |
| matRad 集成 | 开源 MATLAB/Octave TPS（含 PB/CC/MC 引擎与优化器）| 视配置 | 视配置 | 集成层（跨语言 sidecar 服务）| MATLAB Runtime / Octave 兼容性风险 | ⏭ 5b 之前评估 |

## 3. 选型结论

1. **v1（本交付）**：自研解析水等效引擎——为系统打通"束流 → 剂量网格 →
   显示/DVH/导出"全链路，作为后续一切精度改进的基准骨架。
2. **v2（Phase 4 主体）**：Collapsed Cone 卷积叠加 + 逐步补齐
   （HU→ρ/电子密度校准曲线、射线发散、MLC 通量调制、多能级核库）。
3. **MC / LBTE**：不自研。若 v2 精度不足，评估集成现成 MC 引擎或 matRad
   sidecar 服务（跨语言接口成本是主要风险）。

## 4. v1 原型模型（已实现 `doseEngineService.js`）

```
D(v) = Σ_beams  weight_b · TPR(d_rad(v)) · OAR(r_v)      （归一化后）
TPR(d) = (d/dmax)^0.7            d < 1.5 cm（6MV 建成区）
       = exp(−0.047·(d − 1.5))   d ≥ 1.5 cm                ← 6MV 水 TPR 拟合
OAR(r) = ½·[erf((r+W)/s) − erf((r−W)/s)],  s = 8mm/√2     ← X-jaw 平野 + 半影
d_rad  : 平行射线近似，沿束轴两密度模型（HU>−300 → 水 ρ=1，否则 ρ=0）逐层累计
归一化 : 等中心体素 = 处方剂量（isocentre normalisation）
省略（记录在案）：射线发散（平方反比）、HU 异质修正、MLC 通量调制、
                 散射贡献、床/体模衰减
```

- 输入：`referenceDoseFileId`（网格几何基准）+ `planId`（束流与等中心）
- 输出：标准 RTDOSE 文件（`DoseType CALCULATED`、Int32、1e-5 Gy 步长），
  注册进 `dicom_files` —— 显示 / DVH / 点剂量 / 导出全链路自动适用
- 端点：`POST /api/dose-engine/study/:studyId/compute`

## 5. 验证结果（真实 TEST849 数据）

**物理曲线自检（单元测试）**
- TPR(1.5 cm) = 1.000；TPR(10 cm) = 0.671 vs 6MV 文献 PDD(10×10, 10 cm) ≈ 67% ✓
- 平野剖面：中心平坦度 ±2% 内；场边缘 50%；半影外 <1% ✓

**与外部参考网格的空间对比（同几何，归一化到等中心 = 7392 cGy）**

| 指标 | 参考剂量（Eclipse 9f IMRT）| v1 计算（水等效开放野）|
|---|---|---|
| 网格 | 94×182×87 @ 2.5 mm | 同 |
| max | 8084.8 cGy（109% Rx）| 14124 cGy（191% Rx）|
| ref>5 Gy 区域均值 | 3227 cGy | 3811 cGy（+18%）|
| 该区域 RMS 偏差 | — | 3856 cGy |
| 热点侧向剖面（等中心层面）| 6457→7947→6259 cGy | 系统性偏热 1.3-2.0× |

**偏差来源（预期内，v2 逐项消除）**：开放野无 MLC 调制（IMRT 把通量整形得
更"散"）、无异质修正（骨/气腔）、平行射线近似（无发散/散射）、
等中心归一策略差异。

**运行时**：87 帧 × 9 野 × 512² CT 全量重算 ≈ 7.3 s（Node 单线程）。

## 6. 精度声明

v1/v2 原型**不构成临床级剂量计算**。所有对比为相对合理性校验；
临床使用必须依赖外部 TPS 的剂量文件（当前主链路）。

## 7. Phase 4 路线（M5b 前置）

1. v2：Collapsed Cone + 异质修正 + 发散项 + MLC 通量图输入
2. 基准扩充：≥2 个外部病例 + 文献 PDD/OAR 全曲线对比
3. matRad sidecar 集成 spike（仅当 v2 不达标）
4. M5b：逆向优化器（fluence 目标函数 + 序贯二次优化，内核 = v2）

---

## 附录：P4-M2 引擎 v2 基准（2026-09-11）

对照：研究 1（TEST849）plan 12（9 野 DMLC，166 控制点/野，均整器无楔形），
参考剂量 = 外部 TPS RTDOSE（file 92，max 9315 cGy）。计算均在同一参考网格
上未归一化逐体素比较；gamma 前逐网格归一到自身最大值。

### v2 升级项（均已单测，backend/tests/doseV2Math.test.js 16/16）

| 项 | 实现 |
|---|---|
| HU→ρ | 分段线性查表 [−1000→0, 0→1, 100→1.1, 2000→2]（可配置） |
| 射线发散 | 虚拟源（SAD=1000）精确平方反比 + BEV 投影（等中心平面） |
| MLC 通量 | 逐控制点叶对 → BEV 通量，按累积 MeterSet 权重增量积分 |
| TPR 表 | 6MV (µ=0.047, dmax=1.5) / 10MV (µ=0.030, dmax=2.3)，按野能量选择 |
| 深度 | ρ 加权放射学深度（空气不再重置射线路径） |

### 中心轴 PDD（过等中心柱，各自归一化，z 偏移以外部位最大值帧为 0）

| z 偏移(帧) | 外部 | v1 | v2-MLC | v2-开野 |
|---|---|---|---|---|
| −20 | 84.0% | 0.0% | 26.2% | 92.8% |
| −13 | 99.3% | 82.1% | 26.2% | 89.1% |
| −7 | 98.3% | 94.9% | 0.0% | 88.5% |
| 0 | 100.0% | 99.2% | 0.0% | 88.7% |
| +7 | 45.3% | 95.0% | 0.0% | 89.2% |
| +13 | 5.1% | 95.2% | 15.5% | 90.0% |
| +20 | 1.2% | 97.2% | 25.8% | 92.1% |

### 2D gamma（3%/3mm，等中心层面，>10% 区域，逐网格归一）

| 引擎 | 通过率 |
|---|---|
| v1 | 30.4% |
| v2-MLC | 0.0% |
| v2-开野 | 16.7% |

### 如实记录的发现与未决项

1. **v1 的 PDD 形状错误**：v1 在 ±20 帧全程 95%+（无衰减）——平行射线 +
   二值水模型的深度累积在 9 野求和下被抹平；z 剖面与外部剂量相关系数≈0。
   v1 gamma 30.4% 主要来自绝对剂量峰值的偶然重合。
2. **v2-开野（无 MLC）**：z 剖面在 CT 范围内平坦（88-92%）——轴位面内旋转
   的 9 野求和在中轴柱上深度近似 z 无关，属该引擎类别的预期行为；无靶区
   调制 → 与外部调制分布 gamma 16.7%。侧向剖面（等中心行）峰值 87-92%
   位于中心，边缘下降——比 v1 更接近外部的调制形状但缺少中心锐峰。
3. **v2-MLC 的叶行映射未决项**：修复 extractMLC 的 bank 顺序配对错误
   （原实现按交错配对，DICOM 实际为 [bank1×N, bank2×N] 顺序排列；RTPLAN
   导出端同步修正）后，等中心体素所在叶行（y_bev≈0 → row 30）在全部
   9 野全部控制点中从未开启，导致等中心层面剂量为 0。镜像约定被
   跨野行占用相关性否定（A: +0.08 vs B: −0.30）。行→y_bev 的原点偏移/
   符号需真机 TPS/计划系统参照才能定论（离线无判定手段）。
   引擎对 MLC 的机械响应已验证（tx=10mm 等中心偏移的通量重分布可见）。
4. **数据质量**：修复过程中确认 dcmjs datasetToBuffer 不写
   MediaStorageSOPInstanceUID（已在 M5 修复写入端）。

### 结论与下一步

- v2 的物理升级件（ρ 校准、发散、TPR 表）机制与单测齐备；
  MLC 通量通路的机械链路已通，**叶行映射校准是 M2 关闭前的唯一未决项**，
  需要：(a) 一例含已知输出的开放数据（如 TCIA 含 RTDOSE 的 IMRT 病例），
  或 (b) 真机 TPS 导出对照。在校准前 v2-MLC 结果不用于临床判断。
- v2-开野作为"发散 + ρ"增量与 v1 可并存对比（computeAndStoreDose engine 参数）。

